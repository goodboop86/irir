[project]
name = "irir"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "boto3>=1.40.10",
    "fastapi[standard]>=0.116.1",
    "streamlit>=1.48.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.7",
]
